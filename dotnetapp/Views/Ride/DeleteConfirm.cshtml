@model RideShare.Models.Ride

<h2>Delete Ride</h2>

<div>
    <p>Are you sure you want to delete this ride?</p>
    <dl class="row">
        <dt class="col-sm-2">Ride ID:</dt>
        <dd class="col-sm-10">@Model.RideID</dd>
        <dt class="col-sm-2">Departure Location</dt>
        <dd class="col-sm-10">@Model.DepartureLocation</dd>
        <dt class="col-sm-2">Destination</dt>
        <dd class="col-sm-10">@Model.Destination</dd>
        <!-- Include other ride details as needed -->
    </dl>
    @* @using (Html.BeginForm("Delete", "Ride", new { id = @Model.RideID }, FormMethod.Post))
    {

        @Html.HiddenFor(model => model.RideID)
        <input type="submit" value="Delete" class="btn btn-danger" />
        <a asp-action="AvailableRides" class="btn btn-secondary">Cancel</a>
 *@
        @* @Html.ActionLink("Back to List", "Index", null, new { @class = "btn btn-default" }) *@

        <form asp-action="Delete" asp-route-id="@Model.RideID" method="post">
            <input type="submit" value="Delete" class="btn btn-danger" />
            <a asp-action="AvailableRides" class="btn btn-secondary">Cancel</a>
        </form>
    @* } *@

</div>
